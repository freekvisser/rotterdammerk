nelmio_api_doc:
    documentation:
        info:
            title: My App
            description: This is not working!
            version: 1.0.0
    areas: # to filter documented areas
        default: 
            path_patterns:
                - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
        admin:
            path_patterns:
                - ^/admin # Accepts routes under /api except /api/doc
        shop:
            path_patterns:
                - ^/shop # Accepts routes under /api except /api/doc